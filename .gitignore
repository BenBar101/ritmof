# Dependencies
node_modules/

# Build
dist/
*.local

# Env and secrets — never commit
.env
.env.*
!.env.example

# Local data / sync files — keep out of git
data/

# Editor / OS
.cursor/
.DS_Store
.ds_store
*.log
*.swp
.cache/
src.zip

# Playwright local artifacts (do not commit)
test-results/.last-run.json